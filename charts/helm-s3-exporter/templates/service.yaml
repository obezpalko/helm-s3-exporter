apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-s3-exporter.fullname" . }}
  labels:
    {{- include "helm-s3-exporter.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "helm-s3-exporter.selectorLabels" . | nindent 4 }}

