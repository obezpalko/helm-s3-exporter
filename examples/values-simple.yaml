# Simple example for quick setup
# Configure a single public repository

config:
  inline:
    enabled: true
    url: "https://charts.bitnami.com/bitnami/index.yaml"

# Optional: Enable features
serviceMonitor:
  enabled: false

html:
  enabled: false

